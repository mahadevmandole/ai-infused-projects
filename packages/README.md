# Shared packages

This folder contains reusable monorepo packages that can be shared across apps.

## Structure

- `frontend/` for shared UI components, hooks, styles, and utilities
- `backend/` for shared API helpers, services, and domain logic
- `ai/` for shared agents, prompts, RAG utilities, and model integrations
